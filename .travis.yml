sudo: true
language: c
compiler:
- clang
- gcc
install:
- sudo apt-get update
- sudo apt-get install liblog4c-dev libcmocka-dev
before_script:
- sudo sysctl vm.overcommit_memory=1
script: ./configure && make check
